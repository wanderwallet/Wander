Brave screenshot:

- Both the popup and the navbar should be visible.
- Brave's window must be 762px wide so that the popup and the navbar widths match. Still, the caps of the navbar might
  be to be moved in 3-4px on each side to make it narrower.
- Test page background should be #3B3B3F for dark mode and white for light mode.
- Leave a 32px padding around the image when cropping it.

Chrome mobile screenshot:

In app browser screenshot:

