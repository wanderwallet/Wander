Brave screenshot:

- Both the popup and the navbar should be visible.
- Brave's window must be 774px (innerWidth) wide so that the popup and the navbar widths match. Still, the caps of the navbar might
  be to be moved in 3-4px on each side to make it narrower.
- Test page background should be #3B3B3F for dark mode and #FFFFFF for light mode.
- Leave a 32px padding around the image when cropping it.

Chrome screenshot:

- Both the popup and the navbar should be visible.
- Chrome's window must be 1048px (innerWidth, in Dani's Chrome as it depends on the extensions) wide so that the popup and the navbar widths match.
- Test page background should be #3C3C3C for dark mode and #FFFFFF for light mode.
- Leave a 32px padding around the image when cropping it.

Chrome Android screenshot:

- Test page background should be #111510 for dark mode and #F7FBF4 for light mode.

Edge screenshot:

- Both the popup and the navbar should be visible.
- Edge's window must be 831px (innerWidth, in Dani's Chrome as it depends on the extensions) wide so that the popup and the navbar widths match.
- Test page background should be #3B3B3B for dark mode and #F7F7F7 for light mode.
- Leave a 32px padding around the image when cropping it.

In app browser screenshot:

- Test page background should be #1B1D21 for dark mode and #FFFFFF for light mode.
- Leave a 15px padding around the image when cropping it.
