# isomorphic-messaging

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build isomorphic-messaging` to build the library.

## Running unit tests

Run `nx test isomorphic-messaging` to execute the unit tests via [Vitest](https://vitest.dev/).
